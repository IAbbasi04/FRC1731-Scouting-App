# Season scouting configuration

Game-specific scouting fields should live here rather than being hard-coded into analytics logic. A future version will load a season configuration that defines auto, teleop, endgame, penalties, reliability, and subjective fields.

This makes each new FRC season a configuration/update task instead of a rewrite of the scouting app.
